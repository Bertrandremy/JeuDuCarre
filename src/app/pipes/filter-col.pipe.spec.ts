import { FilterColPipe } from './filter-col.pipe';

describe('FilterColPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColPipe();
    expect(pipe).toBeTruthy();
  });
});
